[project]
name = "cancer-detective-tests"
version = "0"
description = "Playwright-based consent tests for the Cancer Detective static site"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8",
    "pytest-playwright>=0.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
